apiVersion: v1
kind: Service
metadata:
  name: ewallet-api
  namespace: ewallet
  labels:
    app: ewallet-api
spec:
  type: ClusterIP
  selector:
    app: ewallet-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
